sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t qudus/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true qudus/react-test npm test

after_success:
  - docker build -t qudus/multi-client ./client
  - docker build -t qudus/multi-nginx ./nginx
  - docker build -t qudus/multi-server ./server
  - docker build -t qudus/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push qudus/multi-client
  - docker push qudus/multi-nginx
  - docker push qudus/multi-server
  - docker push qudus/multi-worker
